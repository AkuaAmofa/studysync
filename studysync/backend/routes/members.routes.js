// Member routes are mounted under /api/groups via groups.routes.js
// This file is kept for potential standalone member route expansion.
const express = require('express');
const router = express.Router();

module.exports = router;
